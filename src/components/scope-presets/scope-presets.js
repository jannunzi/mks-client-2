import React from "react";
import {connect} from "react-redux";

const ScopePresets = ({}) =>
  <div>
    <h1>Scope Presets</h1>
    <div className="mks-shadow-border">
      <h2>TBD</h2>
    </div>
  </div>

export default connect()(ScopePresets)
