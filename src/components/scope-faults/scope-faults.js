import React from "react";
import {connect} from "react-redux";

const ScopeFaults = ({}) =>
  <div>
    <h1>Scope Faults</h1>
    <div className="mks-shadow-border">
      <h2>TBD</h2>
    </div>
  </div>

export default connect()(ScopeFaults)
